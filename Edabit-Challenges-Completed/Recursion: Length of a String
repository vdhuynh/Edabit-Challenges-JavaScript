// Write a function that returns the length of a string. Make your function recursive.

function length(str) {
	if(str === "") return 0;  // Base case (stopping point for recursive function)
	return length(str.substr(1)) + 1; //  + 1 == starting point; length() is called recursively
}
