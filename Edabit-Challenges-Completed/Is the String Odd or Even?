//  Given a string, return true if its length is even or false if the length is odd.

function oddOrEven(s) {
	return s.length % 2 === 0 ? true: false; // using ternary operator and mod with length for true and false
}
